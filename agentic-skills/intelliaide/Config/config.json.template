{
  "use_llm_file_selection": true,
  "file_selection_only": false,
  "exit_after_analysis": false,
  "quiet": false,
  "always_include_kubelet_files": false,
  "must_gather_docs_dir": "DataSource",
  "must_gather_base_dir": "",
  "claude": {
    "auth_type": "gcloud",
    "api_url": "https://aiplatform.googleapis.com",
    "model_id": "claude-opus-4-6",
    "max_tokens": 16384,
    "verify_ssl": true,
    "endpoint_pattern": "https://aiplatform.googleapis.com/v1/projects/YOUR_GCP_PROJECT_ID/locations/global/publishers/anthropic/models/{model_id}:streamRawPredict",
    "custom_gateway": true,
    "price_per_1m_input_tokens": 3.0,
    "price_per_1m_output_tokens": 15.0,
    "api_key": "",
    "max_chunk_tokens": 80000
  },
  "agent": {
    "max_iterations": 25,
    "max_deepening_rounds": 3
  },
  "notes": {
    "use_llm_file_selection": "Set to true to use LLM to analyze user input and suggest priority files; false to use default YAML list only",
    "file_selection_only": "Set to true to run only LLM file selection and exit (no data analysis or RCA); false to run full workflow",
    "exit_after_analysis": "Set to true to exit after YAML and LOG file analysis (no LLM RCA); false to run full workflow including RCA",
    "always_include_kubelet_files": "Set to true to always add kubelet_service.* and kubelet_journal.* to analysis/RCA; false to use only LLM-suggested files",
    "quiet": "Set to true to suppress all prints except one: payload sent to LLM and token/cost summary",
    "must_gather_docs_dir": "Path to must-gather documentation directory (MUST_GATHER_*.md files). Relative paths are resolved from the project root. Default: DataSource",
    "must_gather_base_dir": "Path to the must-gather root folder containing the actual data (namespaces/, cluster-scoped-resources/, host_service_logs/, etc.). Can be absolute or relative to project root. When provided via API request, it overrides this config value.",
    "auth_type": "gcloud = Vertex AI via Application Default Credentials or gcloud user creds; api_key = direct Anthropic or gateway key",
    "api_key": "For auth_type api_key: set key or use ANTHROPIC_API_KEY. For gcloud, leave empty.",
    "api_url": "Vertex: https://aiplatform.googleapis.com. Direct Anthropic: https://api.anthropic.com",
    "endpoint_pattern": "Vertex streaming: .../v1/projects/PROJECT_ID/locations/global/publishers/anthropic/models/{model_id}:streamRawPredict. Replace YOUR_GCP_PROJECT_ID in the template above.",
    "custom_gateway": "true for Vertex and other non-standard Anthropic URLs",
    "model_id": "Vertex Claude id, e.g. claude-opus-4-6 (must match a model enabled in your GCP project)",
    "max_tokens": "Maximum tokens for Claude response (default: 16384 for Opus/Sonnet 4 class)",
    "verify_ssl": "Set to false for self-signed certificates in internal environments. Default: true.",
    "price_per_1m_input_tokens": "USD per 1M input tokens (for cost table display; default 3.0)",
    "price_per_1m_output_tokens": "USD per 1M output tokens (for cost table display; default 15.0)",
    "max_chunk_tokens": "RCA chunking budget for large must-gathers (default 80000)",
    "max_iterations": "Maximum number of tool-call iterations for the ReAct agent loop (default: 25)",
    "max_deepening_rounds": "Maximum deepening rounds HIGH -> MEDIUM -> LOW (default: 3)"
  }
}
